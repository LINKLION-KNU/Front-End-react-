import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css';  // 스타일 파일 가져오기
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
